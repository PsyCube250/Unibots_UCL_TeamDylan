#include <Arduino.h>

// Keep the Arduino sketch as the single source of firmware logic while making
// this directory open directly as a PlatformIO project.
#include "../four_wheel_encoder_demo.ino"
